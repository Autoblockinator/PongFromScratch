{
	"func": {
		"lerp": {
			"cpp": "((diff + shape.getSize().y) / (player->shape.getSize().y + shape.getSize().y) * (vel_transfer_max - -vel_transfer_max) + -vel_transfer_max)",
			"py": "((mem.diff + mem.ball.size) / (mem.player.size + mem.ball.size) * (mem.transmax - -mem.transmax) + -mem.transmax)"
		}
	},
	"settings": {
		"default_hides": [
			".settings"
		]
	},
	"player": {
		"pos": 490.0,
		"size": 100.0
	},
	"ball": {
		"pos": 566.464966,
		"size": 20.0
	},
	"diff": 76.4649658,
	"transmax": 0.5
}